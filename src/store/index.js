/* eslint-disable */
import { createStore } from "vuex";

export default createStore({
    state: {
        username: "",
        token: "",
    },
    getters: {},
    mutations: {},
    actions: {},
    modules: {},
});
